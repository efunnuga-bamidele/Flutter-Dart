void main() {
  var speed = 100;
  final maxSpeed = speed * 4; //Can do runtime initialization using calculation.
  print(maxSpeed);

  const version = '1.0.0'; //Must be set before runtime.
  print(version);
}
